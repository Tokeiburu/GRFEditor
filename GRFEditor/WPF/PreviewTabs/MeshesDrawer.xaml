<UserControl x:Class="GRFEditor.WPF.PreviewTabs.MeshesDrawer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Name="_backgroundGrid">
        <Viewport3D x:Name="_viewport3D1">
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="_primaryCamera"
                    Position="5 4 5" 
                    LookDirection="-1 -1 -1"
                    />
            </Viewport3D.Camera>
            <ModelVisual3D x:Name="_modelLight">
                <ModelVisual3D.Content>
                    <DirectionalLight Direction="-1,-1,-1" />
                </ModelVisual3D.Content>
            </ModelVisual3D>
            <ModelVisual3D x:Name="_modelLight2">
                <ModelVisual3D.Content>
                    <DirectionalLight Direction="-1,-1,-1" />
                </ModelVisual3D.Content>
            </ModelVisual3D>
        </Viewport3D>
        <Grid Name="_primaryGrid" Background="#01000000">
        </Grid>
    </Grid>
</UserControl>
