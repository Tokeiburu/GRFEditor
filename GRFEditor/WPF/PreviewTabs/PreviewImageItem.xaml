<UserControl x:Class="GRFEditor.WPF.PreviewTabs.PreviewImageItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Transparent">
		<Border BorderThickness="1" Name="_borderOverlay" Background="{DynamicResource PreviewItemMouseOverBackgroundBrush}" BorderBrush="{DynamicResource PreviewItemMouseOverBorderBrush}" Visibility="Hidden" />
        <Border BorderThickness="1" BorderBrush="Transparent">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <Image Name="_image" />
                <TextBlock Grid.Row="1" Name="_tbName" TextWrapping="Wrap" HorizontalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
